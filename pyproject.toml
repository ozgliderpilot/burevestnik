[project]
name = "burevestnik"
version = "0.1.0"
description = "Telegram bot posting meteoblue Melbourne CBD forecast"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.40",
    "httpx>=0.25",
    "selectolax>=0.3.20",
    "tzdata; sys_platform == 'win32'",  # zoneinfo needs the IANA db on Windows; on Linux runners it's in the OS
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/burevestnik"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
pythonpath = ["src"]
